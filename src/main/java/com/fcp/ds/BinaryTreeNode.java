package com.fcp.ds;

/**
 * Represents a binary tree node with two pointers:
 * One to the left child and one to the right child.
 * @param <T> Type of the value.
 */
public class BinaryTreeNode<T> {
    T value;

    BinaryTreeNode<T> left;
    BinaryTreeNode<T> right;
}
